## SMLink 方法优势分析

### 1. 图结构

基因调控网络推断的核心目标，是根据单细胞转录组数据与已有调控先验，恢复转录因子与靶基因之间潜在的调控关系。已有研究指出，许多传统监督方法更侧重于对单个基因对进行判别，因而难以充分利用全局基因相关性与网络拓扑信息，忽略了基因之间复杂的调控关系，泛化能力也受到限制。相比之下，将基因调控网络推断统一建模为图上的链接预测问题，更符合 GRN 重建的任务本质。 

SMLink 的首要优势，就在于其并未将调控关系视为孤立的二元分类问题，而是将转录因子、靶基因及其已知相互作用共同纳入统一图结构中进行建模。在这种框架下，模型的目标不再只是判断“某一对基因是否存在关系”，而是结合节点属性与图拓扑，从整体网络层面恢复潜在调控边。该建模方式能够同时捕获局部邻域信息与全局结构信息，因此在理论上更适合复杂的 GRN 推断任务。

### 2. 表达信息与结构信息的协同融合

单细胞 RNA 测序数据能够提供基因在细胞层面的表达状态，但表达相关性本身并不等价于真实调控关系。很多研究也指出，单纯依赖相关系数、互信息或基因对级别特征的方法，往往难以准确刻画基因间复杂且非线性的相互作用。

SMLink 的第二个重要优势在于，它并非只利用表达矩阵作为输入特征，而是将基因表达特征与先验调控图结构共同纳入表示学习过程。基因表达矩阵反映节点状态，已知调控边则提供网络拓扑约束；通过图神经网络的消息传播机制，模型能够在邻域聚合过程中学习一个基因在调控网络中的上下文位置。这一过程使得节点表征不再停留于浅层共表达相似性，而转化为同时包含表达模式与拓扑语义的结构化表示。 

这种融合式表示学习比单纯的 pairwise 建模更具优势。它能够缓解“高相关但非调控”带来的误判风险，并增强模型对于复杂调控模式的识别能力。

### 3. 面向稀疏先验网络的解决方案

GRN 推断中的一个典型难点在于，真实可验证的调控关系获取成本高，导致可用的先验调控边通常十分有限，进而使先验网络表现为稀疏、不完整甚至含噪。，导致先验网络往往具有稀疏、不完整甚至含噪的特点。研究指出，多数 GNN 类方法对已知调控边数量依赖较强，当可利用的先验交互有限或观测网络包含噪声时，其性能会受到明显影响。

SMLink 在方法上针对这一问题进行了专门设计。其双视图机制通过保留原始图并构造扰动图，使模型在训练阶段不只适应“理想图结构”，还能够适应更稀疏、更不完整的图形态。这是因为保留原图有助于最大化利用已有调控信息，而随机删边构造的扰动图则用于模拟先验网络中可能存在的边缺失与结构不完整性。

这种设计的逻辑优势在于，SMLink 学到的不是某一份具体先验图的表面结构，而是节点在图扰动下仍保持稳定的核心表示。因此，当真实应用中先验边较少、标注不足时，模型仍具备较好的适应能力。实验结果同样表明，在极低网络密度条件下，SMLink 在大多数数据集上的 AUPRC 仍优于其他监督或无监督基线，说明这种面向稀疏场景的设计具有实际有效性。

### 4. 一致性表示学习

SMLink 的另一项关键优势体现在其一致性学习思想。与显式依赖负样本分离的对比学习不同，SMLink 更强调跨视图表示的一致性约束，从而在不过度干扰主任务的前提下增强表示稳定性，以此提升节点表征质量。

这种一致性约束的价值主要体现在两个方面。第一，它能够缓解模型对单一图结构的过拟合，使表示更稳定；第二，它能够在标注不足时利用结构扰动带来的自监督信号，增强模型的泛化能力。原始研究中的消融实验表明，引入对比学习后，模型在 AUPRC 上获得提升，同时 AUROC 的标准差下降，说明该策略不仅提升了性能，也改善了训练稳定性。

因此，SMLink 的优势并不只是加入了额外损失函数，而在于它通过一致性学习，使模型真正具备了对图缺失、图扰动和样本不足场景的耐受性。

### 5. 更强的边关系建模能力

对于 GRN 推断而言，模型最终要解决的是“如何判定两个节点之间是否存在调控边”。这一步实际上决定了模型能否将前面学到的节点表示有效转化为高质量的边预测结果。

从生物学的角度来看，TF–target 调控关系并不只是简单的相似性关系，而往往具有方向性、非线性和复杂交互特征。因此，一个优秀的解码模块不能仅仅判断两个节点“是否接近”，还应能够刻画它们之间更丰富的关系模式。SMLink 的突出之处，正在于其边级关系建模更强调对节点对交互的直接刻画。此方法从六个不同的角度刻画两节点之间边的关系，从而使模型在恢复真实调控边时拥有更高的表达能力。

这种优势尤其重要。因为在 GRN 推断任务中，真正困难的往往不是节点表示本身，而是如何利用表示准确地区分真实正边与大量潜在负边。研究结果显示，SMLink 相比 GENELink 等方法在 AUPRC 上更具优势，并能更好地区分正负基因对，这也从侧面说明更强的关系建模与更稳定的表示学习共同提升了边预测质量。

注稀缺”的现实条件下仍保持较强的迁移能力。对于 GRN 推断这种高成本、低标注的任务而言，这一点具有非常重要的意义。

### 6. 对噪声先验图具有更好的容忍性

真实调控网络往往并非完全准确，先验边集合中可能同时存在缺失边与错误边。原始研究进一步通过向观测网络中随机翻转边来模拟噪声场景，结果表明，随着噪声水平升高，所有模型性能都会下降，但 SMLink 在大多数数据集上受影响程度低于 GENELink 等传统算法，在 AUROC 和 AUPRC 上表现出更高的噪声容忍性。

这一现象与 SMLink 的设计逻辑是一致的：当模型在训练中已经通过图扰动与一致性约束学会了“忽略非本质结构变化”，那么它在面对带噪先验网络时，自然更不容易出现表示崩塌或边预测剧烈退化。